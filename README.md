
For Running the server

```
cd code/server
source ../../env/bin/activate
uvicorn main:app --host 0.0.0.0 --port 8000
```

For running the bus simulator
```
cd code/server
source ../../env/bin/activate
python bus_simulator.py
```